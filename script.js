{let numCount = 9
let count = document.querySelector('.count')
function addLike(){
numCount ++
count.innerText = numCount
}}

{let numCount = 12
    let count = document.querySelector('.count-2')
    function addLikee(){
    numCount ++
    count.innerText = numCount
    }}

    {let numCount = 9
        let count = document.querySelector('.count-3')
        function addLikeee(){
        numCount ++
        count.innerText = numCount
        }}
        